# Okta Configuration (Gateway's own Okta domain)
OKTA_DOMAIN=test.oktapreview.com

# Gateway Configuration
GATEWAY_BASE_URL=http://localhost:8000
GATEWAY_PORT=8000

# Logging
LOG_LEVEL=INFO

# Agent Configuration
# Agent credentials are configured in config/config.yaml
# Each agent has:
#   - agent_id: Unique identifier for the MCP client (e.g., "cursor", "claude_code")
#   - client_id: Okta service account client ID
#   - private_key: JWK format private key for signing JWT assertions
#
# Agent credentials are used for:
#   - Token Exchange (RFC8693): Exchange user access token for ID-JAG at org auth server
#   - ID-JAG JWT: Identity assertion for target MCP server
#   - Cross-App Access: Access tokens from multiple target authorization servers
#
# Example config.yaml structure:
# agents:
#   cursor:
#     agent_id: "cursor"
#     client_id: "0oa1234567890abcdef"
#     private_key: '{"kty":"RSA","kid":"abcdef","use":"sig",...}'
#   claude_code:
#     agent_id: "claude_code"
#     client_id: "0oa9876543210fedcba"
#     private_key: '{"kty":"RSA","kid":"fedcba","use":"sig",...}'
